name: musashi-ii

services:
  musashi-ii:
    build: .
    container_name: musashi_ii
    restart: unless-stopped
    ports:
      - "${MUSASHI_II_PORT:-8082}:8082"
    environment:
      PORT: "8082"
    volumes:
      - ./config.json:/app/config.json
