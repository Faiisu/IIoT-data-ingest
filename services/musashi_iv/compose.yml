name: musashi-iv

services:
  musashi-iv:
    build: .
    container_name: musashi_iv
    restart: unless-stopped
    ports:
      - "${MUSASHI_IV_PORT:-8083}:8083"
    environment:
      PORT: "8083"
    volumes:
      - ./config.json:/app/config.json
