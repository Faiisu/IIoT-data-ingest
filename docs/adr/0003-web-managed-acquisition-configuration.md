# Manage supported acquisition settings through the web UI

Operators must be able to edit every supported acquisition setting through the web UI, including the active channels, calibration, and raw-data retention period. Settings are validated before saving and take effect through a controlled stop, drain, and restart of acquisition. This preserves an operator-friendly workflow without changing the meaning of a running data stream partway through a batch.
